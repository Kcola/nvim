-- Copy this file to work_config.lua and fill in your project names.
-- work_config.lua is gitignored so company-specific info stays local.
return {
    -- Repo names (as returned by git remote get-url origin) that use biome for formatting.
    biome_projects = {
        -- "my-repo-name.git",
    },

    -- Repo names that need a custom eslint config with build-scripts rules.
    custom_eslint_projects = {
        -- "my-monorepo",
    },
}
